interface:
  display_name: "Istio"
  short_description: "Repo-specific patterns and workflows for istio"
  default_prompt: "Use the istio repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true